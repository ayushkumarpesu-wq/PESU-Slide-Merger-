# Core dependencies
requests>=2.28.2
beautifulsoup4>=4.12.2
colorama>=0.4.6
pypdf>=3.11.0
python-docx>=0.8.11

# Web Framework for GUI
Flask>=2.3.0
Flask-CORS>=4.0.0

# Windows-specific: Required for curses UI on Windows
windows-curses>=2.3.0; platform_system=="Windows"

# Optional / recommended for conversion (Windows / better fidelity)
python-pptx>=0.6.21
comtypes>=1.1.11


# Environment variables loader
python-dotenv>=1.0.0

# Optional parser for faster/more robust HTML parsing
lxml>=4.9.3

# Optional cross-platform conversion (commercial / optional)
# aspose-slides>=23.12
# This usually provides watermark so keep it as last option

# Voice assistant / Jarvis dependencies
SpeechRecognition>=3.8.1
pyttsx3>=2.90
# PyAudio is often needed for microphone input; on Windows install from wheel if pip fails
PyAudio>=0.2.11

